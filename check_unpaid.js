const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const count = await prisma.sale.count({ where: { paymentStatus: "UNPAID" } }); console.log("UNPAID Sales:", count); } main().catch(console.error).finally(() => prisma.$disconnect());
